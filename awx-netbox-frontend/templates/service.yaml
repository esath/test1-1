apiVersion: v1
kind: Service
metadata:
  name: {{ include "awx-netbox-frontend.fullname" . }}
  labels:
    {{- include "awx-netbox-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "awx-netbox-frontend.selectorLabels" . | nindent 4 }}
