kind: Service
apiVersion: v1
metadata:
  name: the-service
spec:
  selector:
    deployment: hello
  type: LoadBalancer
  ports:
  - name: tcp-8080
    protocol: TCP
    port: 8666
    targetPort: 8080
