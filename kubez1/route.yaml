apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: hello-openshift
spec:
  host: the-deployment.apps.mint-demo.home.noviisi.fi
  port:
    targetPort: 8080
  to:
    kind: Service
    name: hello-openshift
